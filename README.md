# 💱 CurrencyTracker: Инструмент рыночной аналитики (Курсы Валют)

## Описание проекта
Это проект по автоматическому сбору и анализу курсов валют по отношению к российскому рублю. Данные парсятся с официального сайта Центрального Банка РФ. 

Проект полностью решает поставленные задачи:
1. **Функциональность**: Работоспособный HTML-парсер (`BeautifulSoup4`), собирающий табличные данные с сайта и приводящий курсы к 1 единице (так как ЦБ часто выдает курсы за 10, 100 или 1000 единиц валюты).
2. **Код**: Разработан по правилам PEP 8, модульно структурирован и подробно прокомментирован.
3. **Отчет (README.md)**: В этом файле описана архитектура, цель и алгоритм работы.
4. **Тестирование**: В каталоге `tests/` лежат Unit-тесты для ключевой функции парсинга и обработки данных, написанные на `pytest`.
5. **Интерфейс пользователя**: Собран интерактивный веб-дашборд на `Streamlit` с использованием `Plotly` для красивой визуализации диаграмм и графиков.
6. **Дополнительная логика (Оригинальность)**: Внедрен background-планировщик `schedule`, который позволяет собирать исторические данные автономно (каждые 5 часов) без участия человека. 
7. **Анализ**: Pandas-датафреймы вычисляют и выводят статистику (среднее, минимум, максмум) на основе всех снятий.

## Архитектура
* `scraper.py` - слой сборщика. Отвечает за отправку HTTP-запросов (`requests`) и парсинг HTML-таблиц (`BeautifulSoup4`). Полученные данные агрегируются и записываются в CSV-файл (используя `pandas`) с сохранением истории: новые данные просто приписываются вниз.
* `scheduler_run.py` - фоновый сервис на базе библиотеки `schedule`. Вызывает `scraper.py` каждые 5 часов.
* `app.py` - графический веб-интерфейс, который берет сырые CSV-данные из папки `data/` и строит графики с агрегацией и статистикой.

## Инструкция: Установка и Запуск

### Шаг 1. Установка библиотек
Убедитесь, что у вас установлен Python (рекомендуется 3.9+). Выполните в терминале установку пакетов:
```bash
pip install -r requirements.txt
```

### Шаг 2. Запуск сборщика (Фоновый процесс)
Чтобы данные начали собираться каждые 5 часов, запустите:
```bash
python scheduler_run.py
```
*(Скрипт сразу проверит сеть, снимет свежие данные с ЦБ РФ, запишет их и уйдет в спящий режим на 5 часов).*

### Шаг 3. Запуск веб-интерфейса (Аналитика)
В **отдельном окне** терминала (не закрывая сборщик) запустите:
```bash
streamlit run app.py
```
Откроется красивый дашборд. В интерфейсе мы вывели кнопку ручного обновления "Спарсить данные прямо сейчас", на тот случай, если вы хотите проверить работоспособность, не дожидаясь срабатывания таймера каждые 5 часов.

### Шаг 4. Тестирование
Для того чтобы запустить Unit-тесты и продемонстрировать, что код проверен (Требование 4):
```bash
pytest
```
Тест гарантирует, что ЦБ РФ не поменял архитектуру HTML страницы и словарь возвращает правильную структуру с плавающей точкой (float).

## Выводы
Проект продемонстрировал умелое использование библиотек Python, позволил создать полностью автоматизированную систему накопления информационных рядов и представил элегантный интерфейс для анализа экономических данных.
